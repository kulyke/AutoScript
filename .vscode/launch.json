{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Qt Debug",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/${workspaceFolderBasename}.exe",
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
        }
    ]
}